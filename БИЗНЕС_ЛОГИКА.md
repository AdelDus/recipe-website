# Бизнес-логика проекта "Сайт рецептов"

## 📋 Текущая реализованная бизнес-логика

### 1. Система лайков
**Описание:** Пользователи могут выражать свою симпатию к рецептам

**Реализация:**
- Модель: `Recipe.likes` (IntegerField)
- View: `like_recipe()` - увеличивает счетчик
- AJAX: обновление без перезагрузки страницы
- Анимация при клике

**Бизнес-правила:**
- Лайк увеличивает счетчик на 1
- Нет ограничений на количество лайков от одного пользователя
- Счетчик отображается на главной странице и странице рецепта

**Код:**
```python
def like_recipe(request, recipe_id):
    recipe = get_object_or_404(Recipe, id=recipe_id)
    recipe.likes += 1
    recipe.save()
    return JsonResponse({'likes': recipe.likes})
```

---

### 2. Система комментариев
**Описание:** Пользователи могут оставлять отзывы и комментарии к рецептам

**Реализация:**
- Модель: `Comment` (author_name, text, created_at)
- Связь: Many-to-One с Recipe
- Валидация: обязательные поля (имя и текст)
- Сортировка: по дате (новые сверху)

**Бизнес-правила:**
- Имя автора обязательно (max 100 символов)
- Текст комментария обязателен
- Автоматическая дата создания
- Комментарии привязаны к рецепту
- При удалении рецепта удаляются все комментарии (CASCADE)

**Код:**
```python
if author_name and text:
    Comment.objects.create(
        recipe=recipe,
        author_name=author_name,
        text=text
    )
    messages.success(request, 'Комментарий успешно добавлен!')
```

---

### 3. Категоризация рецептов
**Описание:** Рецепты организованы по категориям для удобной навигации

**Реализация:**
- Модель: `Category` (name, description, icon)
- Связь: One-to-Many с Recipe
- Фильтрация: по категориям на главной странице
- 8 предустановленных категорий

**Бизнес-правила:**
- Название категории уникально
- Рецепт может принадлежать только одной категории
- При удалении категории рецепты сохраняются (SET_NULL)
- Категории имеют иконки (эмодзи) для визуализации

**Категории:**
- 🔥 Горячие блюда
- ❄️ Холодные блюда
- 🍲 Супы
- 🍰 Десерты
- 🥤 Напитки
- 🥗 Закуски
- 🥐 Выпечка
- 🥙 Салаты

**Код:**
```python
def home(request):
    category_id = request.GET.get('category')
    if category_id:
        recipes = Recipe.objects.filter(category_id=category_id)
    else:
        recipes = Recipe.objects.all()
```

---

### 4. Структурированное хранение рецептов
**Описание:** Рецепты хранятся с детальной информацией

**Реализация:**
- Основная информация: название, описание, изображение
- Метаданные: время приготовления, количество порций
- Ингредиенты: список с количеством и единицами измерения
- Шаги: пронумерованная инструкция

**Бизнес-правила:**
- Каждый рецепт имеет уникальный ID
- Ингредиенты и шаги связаны с рецептом (CASCADE)
- Шаги автоматически сортируются по номеру
- Изображения хранятся в папке media/recipes/

---

### 5. Временные метки
**Описание:** Отслеживание создания и обновления данных

**Реализация:**
- `created_at` - автоматическая дата создания
- `updated_at` - автоматическая дата обновления
- Применяется к Recipe, Category, Comment

**Бизнес-правила:**
- Даты устанавливаются автоматически
- Нельзя изменить вручную
- Используются для сортировки (новые рецепты сверху)

---

## 🚀 Предлагаемая дополнительная бизнес-логика

### 1. Рейтинговая система ⭐
**Зачем:** Более точная оценка качества рецепта

**Реализация:**
```python
class Rating(models.Model):
    recipe = models.ForeignKey(Recipe, on_delete=models.CASCADE)
    user_ip = models.GenericIPAddressField()  # Для анонимных пользователей
    stars = models.IntegerField(choices=[(i, i) for i in range(1, 6)])
    created_at = models.DateTimeField(auto_now_add=True)
    
    class Meta:
        unique_together = ['recipe', 'user_ip']  # Один голос с IP

# В модели Recipe:
@property
def average_rating(self):
    ratings = self.rating_set.all()
    if ratings:
        return sum(r.stars for r in ratings) / len(ratings)
    return 0
```

**Бизнес-правила:**
- Оценка от 1 до 5 звезд
- Один IP = один голос
- Средний рейтинг рассчитывается автоматически
- Отображение количества оценок

---

### 2. Система сложности 📊
**Зачем:** Помочь пользователям выбрать рецепт по навыкам

**Реализация:**
```python
class Recipe(models.Model):
    DIFFICULTY_CHOICES = [
        ('easy', '😊 Легко'),
        ('medium', '🤔 Средне'),
        ('hard', '😰 Сложно'),
    ]
    difficulty = models.CharField(
        max_length=10,
        choices=DIFFICULTY_CHOICES,
        default='medium'
    )
```

**Бизнес-правила:**
- Три уровня сложности
- Фильтрация по сложности
- Отображение на карточке рецепта

---

### 3. Калькулятор порций 🧮
**Зачем:** Пересчет ингредиентов на нужное количество порций

**Реализация:**
```python
def calculate_portions(recipe, target_servings):
    multiplier = target_servings / recipe.servings
    adjusted_ingredients = []
    for ingredient in recipe.ingredients.all():
        adjusted_quantity = float(ingredient.quantity) * multiplier
        adjusted_ingredients.append({
            'name': ingredient.name,
            'quantity': round(adjusted_quantity, 2),
            'unit': ingredient.unit
        })
    return adjusted_ingredients
```

**Бизнес-правила:**
- Пропорциональный пересчет
- Округление до 2 знаков
- Динамическое обновление на странице

---

### 4. Система тегов 🏷️
**Зачем:** Дополнительная классификация рецептов

**Реализация:**
```python
class Tag(models.Model):
    name = models.CharField(max_length=50, unique=True)
    
class Recipe(models.Model):
    tags = models.ManyToManyField(Tag, blank=True)
```

**Примеры тегов:**
- Вегетарианское
- Веганское
- Безглютеновое
- Для детей
- Диетическое
- Праздничное
- Быстрое

---

### 5. Поиск рецептов 🔍
**Зачем:** Быстрый поиск нужного рецепта

**Реализация:**
```python
def search_recipes(request):
    query = request.GET.get('q', '')
    recipes = Recipe.objects.filter(
        Q(title__icontains=query) |
        Q(description__icontains=query) |
        Q(ingredients__name__icontains=query)
    ).distinct()
    return recipes
```

**Бизнес-правила:**
- Поиск по названию, описанию, ингредиентам
- Регистронезависимый поиск
- Уникальные результаты

---

### 6. Популярные рецепты 📈
**Зачем:** Показать лучшие рецепты

**Реализация:**
```python
# Топ по лайкам
top_liked = Recipe.objects.order_by('-likes')[:10]

# Топ по комментариям
from django.db.models import Count
top_commented = Recipe.objects.annotate(
    comment_count=Count('comments')
).order_by('-comment_count')[:10]

# Недавно добавленные
recent = Recipe.objects.order_by('-created_at')[:10]
```

---

### 7. Валидация данных ✅
**Зачем:** Обеспечить корректность данных

**Реализация:**
```python
class Recipe(models.Model):
    def clean(self):
        if self.cooking_time <= 0:
            raise ValidationError('Время приготовления должно быть больше 0')
        if self.servings <= 0:
            raise ValidationError('Количество порций должно быть больше 0')
```

**Бизнес-правила:**
- Время приготовления > 0
- Количество порций > 0
- Название не пустое
- Изображение обязательно

---

### 8. Статистика рецептов 📊
**Зачем:** Аналитика для администратора

**Реализация:**
```python
def get_statistics():
    return {
        'total_recipes': Recipe.objects.count(),
        'total_comments': Comment.objects.count(),
        'total_likes': Recipe.objects.aggregate(Sum('likes'))['likes__sum'],
        'avg_cooking_time': Recipe.objects.aggregate(Avg('cooking_time'))['cooking_time__avg'],
        'most_popular_category': Category.objects.annotate(
            recipe_count=Count('recipes')
        ).order_by('-recipe_count').first()
    }
```

---

## 🎯 Рекомендации для защиты проекта

### Что подчеркнуть:

1. **Разделение ответственности:**
   - Модели отвечают за данные
   - Views за бизнес-логику
   - Templates за отображение

2. **Валидация данных:**
   - На уровне модели (clean())
   - На уровне формы
   - На уровне view

3. **Оптимизация запросов:**
   - select_related() для ForeignKey
   - prefetch_related() для Many-to-Many
   - Индексы на часто используемых полях

4. **Безопасность:**
   - CSRF защита
   - Валидация входных данных
   - Защита от SQL-инъекций (ORM)

5. **Масштабируемость:**
   - Поддержка PostgreSQL
   - Кэширование (можно добавить)
   - Пагинация (можно добавить)

---

## 📝 Примеры бизнес-правил для обсуждения

1. **Лайки:** Почему без авторизации? (Простота использования vs точность данных)
2. **Комментарии:** Почему без модерации? (Доверие пользователям vs контроль контента)
3. **Категории:** Почему SET_NULL? (Сохранение данных vs целостность)
4. **Изображения:** Почему обязательны? (Визуальная привлекательность)
5. **Время:** Почему в минутах? (Стандартизация vs гибкость)

---

## 🔄 Жизненный цикл рецепта

```
1. Создание (через админку)
   ↓
2. Добавление ингредиентов и шагов
   ↓
3. Публикация на сайте
   ↓
4. Просмотр пользователями
   ↓
5. Получение лайков и комментариев
   ↓
6. Фильтрация по категориям
   ↓
7. Обновление (опционально)
```

---

## 💡 Выводы

Проект содержит базовую, но функциональную бизнес-логику:
- ✅ Взаимодействие с пользователями (лайки, комментарии)
- ✅ Организация данных (категории, структура рецептов)
- ✅ Валидация и безопасность
- ✅ Масштабируемость (PostgreSQL)

Для улучшения можно добавить:
- 🎯 Рейтинговую систему
- 🎯 Поиск и фильтрацию
- 🎯 Систему тегов
- 🎯 Калькулятор порций
- 🎯 Статистику и аналитику
