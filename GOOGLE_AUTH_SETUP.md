# Настройка Google OAuth для проекта

## ✅ Что уже сделано:

1. Установлен `django-allauth`
2. Настроены модели для связи с пользователями
3. Добавлены ограничения: только авторизованные могут создавать рецепты и комментарии
4. Обновлены шаблоны с кнопками входа/выхода

## 🔧 Настройка Google OAuth (для разработки)

### Шаг 1: Создать проект в Google Cloud Console

1. Перейдите на https://console.cloud.google.com/
2. Создайте новый проект или выберите существующий
3. Включите Google+ API

### Шаг 2: Создать OAuth 2.0 credentials

1. Перейдите в "APIs & Services" → "Credentials"
2. Нажмите "Create Credentials" → "OAuth client ID"
3. Выберите "Web application"
4. Настройте:
   - **Authorized JavaScript origins**: 
     ```
     http://localhost:8000
     ```
   - **Authorized redirect URIs** (ВАЖНО - точный URI с /callback/ в конце!):
     ```
     http://localhost:8000/accounts/google/login/callback/
     ```
5. Сохраните Client ID и Client Secret

⚠️ **ВАЖНО**: Redirect URI должен быть **ТОЧНО** `http://localhost:8000/accounts/google/login/callback/`
- Обязательно с `/callback/` в конце
- Без пробелов
- С правильным портом (8000)

### Шаг 3: Добавить credentials в .env

Добавьте в файл `.env`:

```env
GOOGLE_CLIENT_ID=ваш_client_id
GOOGLE_CLIENT_SECRET=ваш_client_secret
```

### Шаг 4: Настроить в Django Admin

1. Запустите сервер: `python manage.py runserver`
2. Перейдите в админку: http://localhost:8000/admin/
3. Перейдите в "Sites" → измените example.com на `localhost:8000`
4. Перейдите в "Social applications" → "Add social application"
5. Заполните:
   - **Provider**: Google
   - **Name**: Google OAuth
   - **Client id**: ваш Client ID
   - **Secret key**: ваш Client Secret
   - **Sites**: выберите localhost:8000
6. Сохраните

## 🎯 Как это работает:

### Для пользователей:

1. Нажимают "🔐 Войти" в навигации
2. Выбирают "Sign in with Google"
3. Авторизуются через Google
4. Автоматически создается аккаунт
5. Могут создавать рецепты и комментарии

### Ограничения:

- **Создание рецептов**: только для авторизованных (`@login_required`)
- **Комментарии**: только для авторизованных (проверка в view)
- **Просмотр**: доступен всем

## 📝 Альтернатива: Без Google OAuth

Если не хотите настраивать Google OAuth, можно использовать обычную регистрацию:

1. Пользователи регистрируются по email
2. Входят через форму логина
3. Все работает без Google

URL для регистрации: `/accounts/signup/`
URL для входа: `/accounts/login/`

## 🔍 Проверка работы:

1. Откройте http://localhost:8000/
2. Нажмите "🔐 Войти"
3. Попробуйте войти через Google или зарегистрироваться
4. После входа появится "👤 ваш_email" и кнопка "Выход"
5. Кнопка "➕ Добавить рецепт" станет доступна

## ⚠️ Важно:

- Без настройки Google OAuth кнопка "Sign in with Google" не будет работать
- Но обычная регистрация/вход по email работает сразу
- Для production нужно изменить redirect URIs на ваш домен
